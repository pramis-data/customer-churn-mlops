The included summaries were copied from the completed profit-aware notebook output.

The analysis was explicitly labelled `same_test_exploratory` because the same final test predictions were used to identify and assess the profit-maximising thresholds. These thresholds require validation on a separate future cohort before operational deployment.

Add the full locally generated `profit_aware_evaluation_outputs` folder before final archiving.
