The completed manual-versus-AutoML comparison notebook was finalised interactively in Azure ML after isolated-environment package repairs.

Add the final executed notebook here before submission and name it:

`manual_automl_comparison.ipynb`

Do not substitute an earlier notebook version that still attempts to load the Azure AutoML `model.pkl` directly in the manual-analysis kernel.
